apiVersion: 1

providers:
  - name: "Kafka Redis Dashboards"
    orgId: 1
    folder: "Kafka Infra"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
